# npm 12 defaults allow-remote to none and wrongly rejects registry tarballs of
# packages with bundleDependencies (aws-cdk-lib) during lockfile-only updates,
# which breaks Renovate. https://github.com/npm/cli/issues/9800
allow-remote=all
